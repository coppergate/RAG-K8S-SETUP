# IDE
.idea/*
!.idea/.gitignore

# Project-specific exclusions
ai-changes/
.kube/
talos/

# Editor backups
nppBackup/

# Logs and transient reports
*.log
osd_prepare_logs.txt
error report from shutdown restart.txt

# Certs and sensitive configs
common_config/k8s/certs/
common_config/k8s/kube/config/kubeconfig
common_config/k8s/talos/config/talosconfig
common_config/k8s/talos/config/controlplane.yaml
common_config/k8s/talos/config/worker.yaml

# Binaries
common_config/k8s/talos/talosctl
common_config/k8s/kube/kubectl
common_config/k8s/krew/krew-linux_amd64
common_config/k8s/krew/krew-linux_amd64.tar.gz

# Remote access configuration — local artifacts only (secrets, kubeconfigs, binaries)
# Track only README.md and the *.sh setup/hook scripts under this dir.
remote-access-configuration/config/

# Cluster state
cluster-replicas.state

# Images
# talos/metal-amd64-omni-images/ handled by talos/ exclusion
