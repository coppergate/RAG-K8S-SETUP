machine:
  network:
    hostname: worker-0
    interfaces:
    - interface: eth0
      # Flat LAN — single management interface.
      dhcp: false
      addresses:
      - 192.168.5.21/16
      routes:
      - network: 0.0.0.0/0
        gateway: 192.168.0.1
