machine:
  udev:
    rules:
      - SUBSYSTEM=="block", ACTION=="add", KERNEL=="vdb", RUN+="/sbin/pvscan --cache %N"
  network:
    hostname: worker-2
    interfaces:
    - interface: eth0
      dhcp: true
    - interface: eth1
      dhcp: false
      addresses:
      - 172.20.0.112/16
