machine:
  network:
    hostname: inference-1
    interfaces:
    - interface: eth0
      dhcp: true
    - interface: eth1
      dhcp: false
      addresses:
      - 172.20.0.121/16
  install:
    image: hierophant.hierocracy.home:5000/siderolabs/installer-inference:v1.12.4
